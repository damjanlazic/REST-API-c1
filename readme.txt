# Stores-Galores REST API

This is built with Flask, Flask-RESTful, Flask-JWT and Flask-SQLAlchemy.

Features:
User registration, authentication

Add/Delete/View Store (via post, delete, get methods)
(when a store is deleted all of its items are deleted automatically)

Add/Delete/Change/View Items (via post, put, delete, get methods)
(by changing the items' store_id items can be transfered between stores)

Deployed on Heroku.
